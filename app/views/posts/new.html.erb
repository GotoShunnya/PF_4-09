<h1>投稿機能フォーム</h1>
<%= form_with model: @post, local: true do |f| %>

  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_area :body %>

  <h4>image</h4>
  <%= f.attachment_field :image %>

  <p><%= f.submit "投稿する" %></p>
<% end %>

<%= link_to "投稿一覧に戻る", posts_path %>